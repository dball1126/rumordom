<%= form_for(current_user.active_relationshipzs.find_by(followedz_id: @business.id),
             html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "Unfollow", class: "btn" %>
<% end %>