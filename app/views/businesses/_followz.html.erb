<%= form_for(current_user.active_relationshipzs.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :followedz_id, @business.id %></div>
  <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>