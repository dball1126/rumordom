<header class="navbar navbar-fixed-top myNavbar">
  <div class="container">
    <nav>
      <ul class="nav navbar-nav myNavbarz">
        <li><%= link_to "Rumordom", root_path %></li>
      <% if logged_in? %>
      <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Menu<span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Businesses", businesses_path, id: "links" %></li>
              <li><%= link_to "Users", users_path, id: "links" %></li>
              </ul>
              </li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Account<span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Profile", current_user, id: "links" %></li>
              <li><%= link_to "Settings", edit_user_path(current_user), id: "links" %></li>
              <li><%= link_to "Stats", stats_path(current_user), id: "links" %></li>
              <li class="divider"></li>
              <li>
                <%= link_to "Log out", logout_path, method: :delete, id: "links" %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "Log in", login_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>