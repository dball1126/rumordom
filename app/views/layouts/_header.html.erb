<header class="navbar navbar-fixed-top myNavbar">
  <div class="container">
    <nav>
      <ul class="nav navbar-nav myNavbarz">
        <li><font color="#03ff03" size="4em" font-weight: "bold">Rumor</font><font color="#ff0303" size="4em" font-weight: "bold">dom</font></li>
        <li><%= link_to "Businesses", businesses_path, id: "links" %></li>
      <% if logged_in? %>
          <li><%= link_to "Users", users_path, id: "links" %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Account <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Profile", current_user, id: "links5" %></li>
              <li><%= link_to "Settings", edit_user_path(current_user), id: "links5" %></li>
              <li><%= link_to "Stats", stats_path(current_user), id: "links5" %></li>
              <li class="divider"></li>
              <li>
                <%= link_to "Log out", logout_path, method: :delete, id: "links5" %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "Log in", login_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>